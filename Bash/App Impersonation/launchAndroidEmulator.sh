#!/bin/bash
# Changes directory to the install path that the App Impersonation - Android policy creates
# Runs the emulator command to launch the Rover avd

 
cd /Users/Shared/AI-Android/emulator
./emulator -avd Rover
