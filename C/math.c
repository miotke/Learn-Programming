#include <stdio.h>

int main(void) {
  int x = 3;
  int y = 3;
  int a = 1;
  int b = 10;
  int m = 100;
  int n = 1000;

  printf("%i + %i = %i\n", x, y, x + y);
  printf("%i - %i = %i\n", a, b, a - b);
  printf("%i / %i = %i\n", m, n, m / n);
  printf("%i * %i = %i\n", m, n, m * n);
}
